
export type Studant = {
    id: string,
    name: string,
    email: string,
    birthDate: Date
}

export type Teacher = {
    id: string,
    name: string,
    email: string,
    birthDate: Date
}

export type Class = {
    id: string,
    name: string,
    startDate: Date,
    endDate: Date
}
