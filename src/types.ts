export type Studant = {
    id: number,
    name: string,
    email: string,
    birthDate: Date,
    class_id?: number
}

export type Teacher = {
    id: number,
    name: string,
    email: string,
    birthDate: Date,
    class_id?: number
}